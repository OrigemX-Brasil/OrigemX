# Teste de carga — OrigemX

Gerado em 2026-08-05T01:20:20.572Z · commit `17c49c1`

## Leia isto antes dos números

**O banco medido é o de desenvolvimento, tier Nano do Supabase** — `shared_buffers` 224 MB, `effective_cache_size` 384 MB, `max_connections` 60. É a instância mais fraca disponível, com CPU compartilhada. **Estes números não representam a produção do cliente**: num tier maior, a mesma carga tende a produzir latências menores.

**O gerador de carga e a aplicação rodam na mesma máquina**, disputando CPU. Parte da latência medida é contenção local, não custo do produto.

Ambas as limitações são do ambiente disponível, não escolhas do teste. Estão aqui para que nenhum número abaixo seja lido como se viesse de infraestrutura de produção.

## Alvo da medição

| | |
|---|---|
| Aplicação | build de **produção** (`next build` + `next start`) |
| Por que produção | ISR, cache de página pública e render estático só existem nela |
| Banco | Supabase de desenvolvimento, tier Nano |
| Rampa | 10 → 25 → 50 usuários virtuais |
| Duração | 15.0 min |
| Requisições | 14.830 |

## Volume semeado, confirmado antes de medir

| Item | Quantidade |
|---|---|
| Usuários | 5.034 |
| Canis | 5.000 (1.000 publicados) |
| Cães | 50.000 (12.500 publicados) |
| Vínculos de parentesco (FK) | 87.500 |
| **Linhas de ancestral percorríveis** | **1.487.500** |

> O contrato pede "500.000 registros relacionais de pedigree". O schema não tem tabela de pedigree — parentesco são as colunas `sire_id` e `dam_id` em `dogs`, por invariante do projeto (nunca copiar dado do ancestral). O número equivalente é a quantidade de **linhas de ancestral que a árvore de 5 gerações produz quando percorrida**, e ele supera o acordado. O modelo foi conferido por amostragem contra a função `dog_pedigree` real, camada a camada.

## Resultado por fluxo

| Fluxo | Reqs | p50 | p95 | p99 | Critério | Veredito |
|---|---|---|---|---|---|---|
| 1. Criação e autenticação de sessão | — | 188 ms | **604 ms** | 667 ms | p95 ≤ 2000 ms | ✅ aprovado |
| 2. Listagem paginada de cães | — | 191 ms | **218 ms** | 278 ms | p95 ≤ 2000 ms | ✅ aprovado |
| 3. Busca de cão ou ancestral | — | 190 ms | **213 ms** | 251 ms | p95 ≤ 2000 ms | ✅ aprovado |
| 4. Perfil público por URL / QR | — | 3 ms | **381 ms** | 409 ms | p95 ≤ 2000 ms | ✅ aprovado |
| 5a. Cadastro de cão | — | 562 ms | **610 ms** | 814 ms | p95 ≤ 3000 ms | ✅ aprovado |
| 5b. Atualização de cão | — | 192 ms | **578 ms** | 609 ms | p95 ≤ 3000 ms | ✅ aprovado |
| 6. Pedigree de 5 gerações | — | 3 ms | **390 ms** | 424 ms | p95 ≤ 2000 ms | ✅ aprovado |

> Coluna **Reqs** vazia significa que a corrida é anterior à declaração dos submétricos `http_reqs{fluxo:…}` no k6 — não que o fluxo não rodou. Os percentis da linha vêm de amostras reais.

## Critérios de aprovação

| Critério | Medido | Limite | Veredito |
|---|---|---|---|
| Taxa de erro | 3.26% | < 1% | ❌ **REPROVADO** |
| p95 leituras | 391 ms | ≤ 2000 ms | ✅ |
| p95 gravações | 596 ms | ≤ 3000 ms | ✅ |

## Cache das páginas públicas

As rotas `/d/` e `/c/` são servidas por ISR. Um teste que só pegasse HIT mediria o cache e chamaria de produto, então o custo do MISS aparece em linha separada — e o fluxo de pedigree mira cães nunca visitados justamente para forçá-lo.

| | Valor |
|---|---|
| Taxa de HIT | 75.6% |
| p95 público **HIT** | 7 ms |
| p95 público **MISS** | 407 ms |
| p95 pedigree **MISS** | 414 ms |

A taxa de HIT deste teste é **baixa de propósito**: o script sorteia entre centenas de cães distintos e o cache começa vazio a cada build. Em produção, com uma feira concentrando acessos nos mesmos animais, ela é muito maior — o p95 de HIT acima é o que aquele visitante encontra.

## Consultas mais caras sob carga

Capturado de `pg_stat_statements`, zerado imediatamente antes da rodada.

| Chamadas | Total | Média | Máx | Consulta |
|---|---|---|---|---|
| 1585 | 1984.4 ms | 1.25 ms | 6.0 ms | `WITH pgrst_source AS (SELECT pgrst_call.pgrst_scalar FROM (SELECT $1 AS json_data) pgrst_payload, LATERAL (SELECT "p_owner_id" FROM json_to_record(pgrst_payload` |
| 17308 | 558.8 ms | 0.03 ms | 1.5 ms | `select set_config('search_path', $1, true), set_config($2, $3, true), set_config('role', $4, true), set_config('request.jwt.claims', $5, true), set_config('requ` |
| 414 | 287.2 ms | 0.69 ms | 11.5 ms | `UPDATE "users" AS users SET "last_sign_in_at" = $1, "updated_at" = $2 WHERE users.id = $3` |
| 7107 | 223.1 ms | 0.03 ms | 2.8 ms | `select set_config('search_path', $1, true), set_config($2, $3, true), set_config('role', $4, true), set_config('request.jwt.claims', $5, true), set_config('requ` |
| 414 | 134.2 ms | 0.32 ms | 12.0 ms | `INSERT INTO "sessions" ("aal", "created_at", "factor_id", "id", "ip", "not_after", "oauth_client_id", "refresh_token_counter", "refresh_token_hmac_key", "refres` |
| 414 | 105.2 ms | 0.25 ms | 3.1 ms | `SELECT users.aud, users.banned_until, users.confirmation_sent_at, users.confirmation_token, users.confirmed_at, users.created_at, users.deleted_at, users.email,` |
| 414 | 75.4 ms | 0.18 ms | 4.4 ms | `INSERT INTO "refresh_tokens" ("created_at", "instance_id", "parent", "revoked", "session_id", "token", "updated_at", "user_id") VALUES ($1, $2, $3, $4, $5, $6, ` |
| 414 | 56.0 ms | 0.14 ms | 4.3 ms | `INSERT INTO mfa_amr_claims(id, session_id, created_at, updated_at, authentication_method) values ($1, $2, $3, $4, $5) ON CONFLICT ON CONSTRAINT mfa_amr_claims_s` |
| 415 | 49.1 ms | 0.12 ms | 2.6 ms | `SELECT identities.created_at, identities.email, identities.id, identities.identity_data, identities.last_sign_in_at, identities.provider, identities.provider_id` |
| 414 | 12.3 ms | 0.03 ms | 1.2 ms | `SELECT sessions.aal, sessions.created_at, sessions.factor_id, sessions.id, sessions.ip, sessions.not_after, sessions.oauth_client_id, sessions.refresh_token_cou` |

## Veredito

**REPROVADO em ao menos um critério.** A causa está na seção seguinte. Nenhum parâmetro do teste foi ajustado para produzir aprovação.

## Causa da reprovação

**As 483 falhas são todas do login, e nenhuma delas chegou à aplicação.** O k6 concentrou 100% dos erros na verificação `login 200`; as outras 9 passaram sem uma única falha. Como `autenticar()` devolve `null` e aborta o fluxo quando o login não volta 200, essas requisições nem chegaram a ser feitas contra o Next.

O login do teste vai **direto ao Supabase** (`/auth/v1/token`), não pela aplicação. Descontadas as 897 tentativas de login, a aplicação respondeu **13.933 requisições com 0,00% de erro**.

### O que a medição isolada mostrou

`npm run loadtest:diagnose` dispara logins em sequência contra o mesmo endpoint, com usuários **distintos** a cada tentativa — é o que separa um limite por conta de um limite por IP.

| | |
|---|---|
| Status da falha | **429** |
| Mensagem | `Request rate limit reached` |
| Aceitos antes da primeira recusa | 33 em 8.6s |
| Rodada 1 (rajada) | 34/40 |
| Rodada 2 (após 65s de pausa) | 10/10 |

Recupera integralmente depois da pausa, e usuários diferentes falham do mesmo jeito: **é limite de taxa por IP do Supabase Auth**, não credencial, não capacidade do banco, não a aplicação.

### A confirmação independente, vinda do banco

`UPDATE users SET last_sign_in_at` aparece no `pg_stat_statements` com **414 chamadas** — exatamente o número de logins aceitos. `INSERT INTO sessions` idem. **O Postgres nunca viu as outras tentativas**: foram recusadas na borda, antes de virar trabalho de banco. Se a causa fosse capacidade, o banco teria visto as 897 e falhado em algumas.

### O que isto significa para o critério

A rampa acordada concentra 50 usuários virtuais **num único IP**. Cinco mil criadores reais chegam de milhares de IPs, e cada um autentica uma vez por sessão, não a cada fluxo. O limite que reprovou este teste é do **gerador de carga**, não uma condição que a produção reproduza.

Isso **não** torna o resultado aprovado: o critério é 1% e o medido é 3.26%. Fica registrado como reprovado, com a causa identificada.

**Os caminhos, para decisão do dono do produto:**

1. **Elevar o limite de taxa de auth** no painel do Supabase durante a medição. Remove uma restrição artificial do teste; é a opção que mede o produto. Exige mudança de configuração no projeto.
2. **Medir a autenticação em separado**, com orçamento de erro próprio, e manter o critério de 1% para os cinco fluxos que passam pela aplicação. Muda o desenho do teste e precisa de acordo com o cliente.
3. **Manter como está** e registrar a reprovação com esta causa anexada.

Nenhum deles foi aplicado. Ajustar o teste para produzir aprovação foi vetado explicitamente, e escolher entre eles é decisão de produto.
